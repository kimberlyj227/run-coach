const config = {
	miniDrawer: false,
	fontFamily: `Montserrat, sans-serif`,
	borderRadius: 8,
	outlinedFilled: true,
	presetColor: 'default',
	i18n: 'en',
	container: false
};

export default config;
